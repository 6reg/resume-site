---
interface Props {
  degree: string;
  school: string;
  year: string;
}

const { degree, school, year } = Astro.props;
---

<div class="bg-gradient-to-r from-neutral-50 to-transparent rounded-lg p-4 border border-neutral-100">
  <div class="flex flex-col sm:flex-row sm:justify-between sm:items-start gap-2">
    <div class="flex-1">
      <h3 class="font-semibold text-neutral-900">{degree}</h3>
      <p class="text-neutral-600">{school}</p>
    </div>
    <p class="text-accent font-medium text-sm bg-accent/10 px-3 py-1 rounded-full whitespace-nowrap">{year}</p>
  </div>
</div>
